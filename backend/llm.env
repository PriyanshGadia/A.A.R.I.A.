# ===================================================================
# == A.A.R.I.A. Environment Configuration
# ===================================================================

# -------------------------------------------------------------------
# !! CRITICAL SECURITY SECRETS !!
# -- Replace these placeholders with your own generated secrets.
# -- Do NOT commit this file to a public repository.
# -------------------------------------------------------------------
# Master password for encrypting the database. Must be remembered.
AARIA_MASTER_PASSWORD="GADIA@123"

# A secure key for signing interaction audit logs (HMAC).
# Generate one with: openssl rand -base64 32
INTERACTION_HMAC_AUDIT_KEY="00000000"

# A secure key for encrypting autonomy audit logs (AES).
# Generate one with: openssl rand -base64 32
AUTONOMY_AES_AUDIT_KEY="00000000"


# -------------------------------------------------------------------
# LLM CONNECTION (Local Ollama Setup - PRIMARY)
# -------------------------------------------------------------------
# The API endpoint for your local LLM server (Ollama default).
OLLAMA_API_BASE="http://localhost:11434/v1"
OLLAMA_MODEL="llama3:latest"

# Fallback providers (comma-separated)
# GROQ_API_BASE=https://api.groq.com/openai/v1
# GROQ_MODEL=llama-3.1-8b-instant

# Primary provider to use
PRIMARY_PROVIDER="ollama"

# API key for Groq (leave empty if using only Ollama)
GROQ_API_KEY=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

# Timeout in seconds for API requests. Increased for slow local models.
LLM_TIMEOUT="300"

# Set to "true" to see detailed request/response logs from the LLM adapter.
LLM_DEBUG_LOGGING="false"


# -------------------------------------------------------------------
# CORE SERVICE CONFIGURATION
# -------------------------------------------------------------------
# -- Interaction Core --
# Mode for the rate limiter: 'sliding' or 'token_bucket'.
INTERACTION_RATE_LIMITER_MODE="sliding"
# Session lifetime in seconds (1 hour = 3600).
INTERACTION_SESSION_TTL="3600"
# Use the asyncio-based outbound worker for messages.
INTERACTION_ASYNC_MODE="false"

# -- Cognition Core --
# Max reasoning requests per minute for the cognitive engine.
COGNITION_RATE_LIMIT="30"
# How often in seconds to perform self-reflection.
COGNITION_REFLECTION_FREQUENCY="300"

# -- Autonomy Core --
# Number of autonomous actions that can run at the same time.
AUTONOMY_MAX_CONCURRENT="3"


# -------------------------------------------------------------------
# FILE PATHS & STORAGE
# -------------------------------------------------------------------
# Path to the SQLite database file.
STORAGE_DB_PATH="assistant_store.db"

# Path to the file containing the encrypted master key.
STORAGE_WRAPPED_KEY_PATH="master_key.wrapped"